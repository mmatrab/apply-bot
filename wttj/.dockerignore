# By default, ignore everything
*
# Add exception for the directories you actually want to include in the context
!requirements.txt
# source files
!*.py

# Data file used by the bot
!applied_jobs_list.json

# Never bake local credentials into the image
cred.json